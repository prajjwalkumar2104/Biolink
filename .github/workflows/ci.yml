name: BioLink CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Biolink-backend/package-lock.json

      - name: Install Dependencies
        working-directory: ./Biolink-backend
        run: npm install

      - name: Generate Prisma Client (Mock)
        working-directory: ./Biolink-backend
        run: DATABASE_URL="postgresql://dummy" npx prisma generate

      - name: Compile TypeScript
        working-directory: ./Biolink-backend
        run: npm run build

  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Biolink-frontend/package-lock.json

      - name: Install Dependencies
        working-directory: ./Biolink-frontend
        run: npm install

      - name: Build Next.js
        working-directory: ./Biolink-frontend
        run: npm run build